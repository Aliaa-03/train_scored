import 'package:flutter/material.dart';

class PaymentOrPlatformForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Payment or Platform Return')),
      body: Center(child: Text('Payment or Platform Form')),
    );
  }
}
