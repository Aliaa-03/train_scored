import 'package:flutter/material.dart';

class CoachCleaningForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Coach Cleaning')),
      body: Center(child: Text('Coach Cleaning Form')),
    );
  }
}
