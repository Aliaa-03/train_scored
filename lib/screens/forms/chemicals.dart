import 'package:flutter/material.dart';

class ChemicalsForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Details for Chemicals')),
      body: Center(child: Text('Chemicals Form')),
    );
  }
}
