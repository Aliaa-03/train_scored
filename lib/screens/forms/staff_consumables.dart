import 'package:flutter/material.dart';

class StaffConsumablesForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Staff Consumables')),
      body: Center(child: Text('Staff Consumables Form')),
    );
  }
}
